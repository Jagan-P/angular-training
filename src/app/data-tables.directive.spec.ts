import { DataTablesDirective } from './data-tables.directive';

describe('DataTablesDirective', () => {
  it('should create an instance', () => {
    const directive = new DataTablesDirective();
    expect(directive).toBeTruthy();
  });
});
