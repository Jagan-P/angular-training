import { BindingListenerDirective } from './binding-listener.directive';

describe('BindingListenerDirective', () => {
  it('should create an instance', () => {
    const directive = new BindingListenerDirective();
    expect(directive).toBeTruthy();
  });
});
